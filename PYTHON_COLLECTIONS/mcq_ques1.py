colors = ["red", "green", "burnt sienna", "blue"]
a = "yellow" in colors
print(a)   # it will print a boolean values, as, in operator is used to check if the value is present in the string, if, true then it returns true, and if false it returns, false simply 





print((1,2) + (3, 4))
# the output will contatenate both of these tuples to a single tuple, as the + operator  between these two tuples will concatenate both of these tuples 

